# Local environment files (keep Firebase config out of the repo)
.env
env.js

# Dependencies / build output (in case the project gains a build step later)
node_modules/
dist/

# OS / editor cruft
.DS_Store
Thumbs.db
